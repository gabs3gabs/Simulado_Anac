<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simuladoanac">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimuladoAnac">
        <activity android:name=".questoes.QuestoesTecActivity"></activity>
        <activity android:name=".questoes.QuestoesRegActivity" />
        <activity android:name=".questoes.QuestoesTeoActivity" />
        <activity android:name=".questoes.QuestoesMetActivity" />
        <activity android:name=".questoes.QuestoesNavActivity" />
        <activity android:name=".resultado.PontuacaoActivity" />
        <activity android:name=".questoes.QuestoesActivity" />
        <activity android:name=".info.InfoNavActivity" />
        <activity android:name=".registro.CriarContaActivity" />
        <activity android:name=".registro.RecuperarActivity" />
        <activity android:name=".registro.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>